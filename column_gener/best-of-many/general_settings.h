#pragma once
#include <iostream>
#include <stack>
#include <vector>
#include <tr1/unordered_set>
#include <tr1/unordered_map>
#include <limits>
#include "data_structure.h"
#include "graph.h"

#define elif else if
//#define MIN(X,Y) ((X) < (Y) ? (X) : (Y))

using namespace std;
